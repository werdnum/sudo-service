apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: sudo-service-executor-job-restrictions
spec:
  policyName: sudo-service-executor-job-restrictions
  validationActions:
    - Deny
