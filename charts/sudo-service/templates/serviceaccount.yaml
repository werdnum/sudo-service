apiVersion: v1
kind: ServiceAccount
metadata:
  name: sudo-service-controller-sa
  namespace: {{ .Values.namespace }}
---
# Bound to cluster-admin via ClusterRoleBinding. Only ever attached to the
# ephemeral executor Job pod, never to the long-running controller.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sudo-service-executor-sa
  namespace: {{ .Values.namespace }}
