apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: sudo-service-executor-pod-restrictions
spec:
  policyName: sudo-service-executor-pod-restrictions
  validationActions:
    - Deny
